let hex = document.querySelectorAll(".hexagons path");


for (let i = 0; i < hex.length; i++) {
    // hex[i].setAttribute("stroke", "#dbf0e0")
    hex[i].setAttribute("stroke-opacity", "0.5")
}